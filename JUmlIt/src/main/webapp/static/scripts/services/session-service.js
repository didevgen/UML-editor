'use strict';
angular.module('jumlitApp').factory('Session', function(Utils) {
    var Session = {
        data: {},
        // maybe later
        save: function () {

        }
    };
    return Session;
});
